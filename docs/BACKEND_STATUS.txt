API
----------------------------------------
API endpoint: /send-email/contact

Status: Working

Fix required:
- Problem: Missing first and last name, email, phone number, and message can cause errors
  Fix: Return a status code 400 for invalid input

- Problem: Missing response status code 500 (internal server error) and response message for status code 200 and 500
  Fix: Add status code 500 and response messages

Vulnerabilities:
- Problem: Spam
  Fix: Validate email

Nice to have:
- Extra protection for SQL injection such as the least privilege principle (PoLP), validate incoming request data using a framework such as Zod

Email password: Requires creating an app password.
Steps for creating an app password:
1. Go to Google Account - this can be achieved by going to google.ca, clicking on your profile icon, and clicking Manage your Google Account
2. In the search bar, search "app password" and click on it
3. Create an app name and then click "Create”
4. Copy the 16-character password and paste it in .env's APP_MAILING_PASSWORD

Date: Nov 28, 2026
